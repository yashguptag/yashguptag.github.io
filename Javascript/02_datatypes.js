"use strict"; //treat all JS code as a newer version

// alert( 3 + 3 ) // we are using nodejs, not browser

// console.log(3 + 3) console.log("Yash") this will give you a fucking error because of missing semi colon in first console, & anyway general readability of code should be high bro, not shooting from your ass.

/* console.log(3 
 +
  3) don't do this bull behaviour */

  let name = "yash"
  let age  = 18
  let isLoggedIn = false
   

  // Primitive Data Types---
  // number , range is 2 to power 53
  // bigint
  // string, use in double quotes
  // boolean, ha ya na
  // null, standalone value bhi hai
  // undefined 
  // symbol, to identify the uniqueness of different components, used a lot in react and tools like figma

  // Object---
console.log(typeof undefined); //undefined
console.log(typeof null); //object